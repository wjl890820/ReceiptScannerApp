# 设置页 / 反馈 / Pro 边界说明

## 设置页结构

- **发送反馈**：跳转到 `app/(tabs)/feedback.tsx`，通过 `lib/feedbackService.ts` 调用 Supabase Edge Function `send-feedback`（本仓库不包含该函数源码，部署状态需人工确认）。\n- **高级洞察 Pro**：跳转到 `app/(tabs)/pro-insight.tsx`，仅展示文案和预告，不包含支付/订阅逻辑。`settings.pro.subtitle` 已在文案中标明「即将推出 / coming soon / 近日公開」。\n- **隐私政策**：优先尝试打开 `PRIVACY_POLICY_URL`；若无法打开则回退到内置隐私说明弹窗。\n- **关于**：展示应用名称与版本号，不含额外逻辑。\n\n## 反馈通道边界\n\n- 前端入口：设置页 →「发送反馈」→ `feedback.tsx`。\n- 提交行为：调用 `submitFeedback({ message, email? })`，由 `feedbackService` 发送到 `send-feedback` Edge Function。\n- 错误提示：根据错误 message 匹配「网络错误 / 服务器错误 / 未配置 / 响应异常」，对应到 `feedback.error.*` 文案；提交失败时不清空用户输入，允许重试。\n- 风险：若 Supabase `send-feedback` 未部署或未正确配置，用户会持续看到「提交失败（未配置）」或「服务器错误」。该链路依赖后端，需在运维侧确认部署状态。\n\n## Pro 区域现状\n\n- 当前阶段 Pro **仅为占位与功能预告**：\n  - Settings 中 Pro 区块仅导航到 Pro 页面；\n  - Pro 页面仅展示免费版已有能力与 Pro 潜在功能列表，以及「即将推出」文案；\n  - 不包含任何支付、订阅、权限控制逻辑。\n- 若未来接入订阅：\n  - 建议在 Pro 页面新增明确的 CTA 与状态展示（未订阅/已订阅）；\n  - 将计费与解锁逻辑集中在单独的 billing/service 层，而非散落在页面中。\n\n## send-feedback 的工程风险\n\n- 仓库内无 `supabase/functions/send-feedback` 源码；`ENGINEERING.md` 中已记录该函数需人工确认部署。\n- 若后续决定下线该功能，建议：\n  - 设置页中隐藏「发送反馈」入口，或改为跳转到应用商店评价 / 邮件链接；\n  - 保留 `feedback.tsx` 代码以便日后重启该通道。\n*** End Patch```}"/>
